<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <title>Custom Wordle Scores</title>
    <link rel="stylesheet" href="./style.css" />
    <script type="module" src="./app.js"></script>
  </head>
  <body>
    <div class="container mx-auto p-20" >
      <div data-controller="theme-generator output-clipboard preferences-saver">
        <h1 class="text-cyan-400 text-4xl text-center pb-4">Custom Wordle Scores</h1>
        <div class="flex flex-col lg:flex-row justify-center">
          <div class="px-10 lg:w-72 space-y-4">
            Customise your tiles
            <div class="border border-gray-300 rounded-lg p-2">
              <label for="miss">Miss 🟨</label>
              <select class="w-full" data-preferences-saver-target="input" id="miss" data-action="theme-generator#change" data-theme-generator-target="miss">
                <% emojis do |emoji| %>
                  <option value="<%=emoji.emoji%>" <%= emoji.emoji == "🤏" ? "selected" : "" %>><%=emoji.display_name%></option>
                  <% end %>
                </select>
              </div>
              <div class="border border-gray-300 rounded-lg p-2">
                <label for="wrong">Wrong ⬛️</label>
                <select class="w-full" data-preferences-saver-target="input" id="wrong" data-action="theme-generator#change" data-theme-generator-target="wrong">
                  <% emojis do |emoji| %>
                    <option value="<%=emoji.emoji%>" <%= emoji.emoji == "❌" ? "selected" : "" %>><%=emoji.display_name%></option>
                    <% end %>
                  </select>
                </div>
                <div class="border border-gray-300 rounded-lg p-2">
                  <label for="hit">Hit 🟩</label>
                  <select class="w-full" data-preferences-saver-target="input" id="hit" data-action="theme-generator#change" data-theme-generator-target="hit">
                    <% emojis do |emoji| %>
                      <option value="<%=emoji.emoji%>" <%= emoji.emoji == "✅" ? "selected" : "" %> ><%=emoji.display_name%></option>
                    <% end %>
                  </select>
                </div>
                <div class="flex space-x-2">
                  <button class="bg-cyan-400 border p-1 rounded border-black text-white text-xs" data-action="preferences-saver#clearPreferences">Clear Saved Theme</button>
                  <button class="bg-cyan-400 border p-1 rounded border-black text-white text-xs" data-action="preferences-saver#saveToPreferences">Save Theme</button>
                </div>
              </div>
              <div class="flex flex-col lg:flex-row text-center space-x-4">
                <div>
                  Paste score here 👇
                  <textarea rows=9 class="block w-36 resize-none mx-auto text-center border border-gray-300 rounded-lg" data-theme-generator-target="input" data-action="theme-generator#change"></textarea>
                </div>
                <div class="space-y-4">
                  Themed score
                  <textarea rows=9 readonly class="block w-36 resize-none mx-auto text-center border border-gray-300 rounded-lg" data-theme-generator-target="output" data-output-clipboard-target="input"></textarea>
                  <div class="flex flex-col items-center">
                    <button class="bg-cyan-400 border p-1 rounded border-black text-white text-xs" data-action="output-clipboard#copyToClipboard">Copy to clipboard</button>
                  </div>
                </div>
              </div>
            </div>
            <div class="flex justify-center">
              <span id="flash-message" class="opacity-0 transition-opacity"></span>
            </div>
          </div>
        </div>
      </body>
    </html>
